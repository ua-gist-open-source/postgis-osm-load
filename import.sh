#!/bin/bash

# Enter all your commands in this file. 
# Use a '#' at the beginning of the line to leave a comment.
# To run this later, type this in a terminal window (within the backtick quotes ``)

# `bash import.sh`
